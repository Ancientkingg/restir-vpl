

constexpr auto BASE_LIGHT_INTENSITY = 100.0f;

constexpr auto ENABLE_TEXTURES = false;

constexpr auto RENDER_FRAME_COUNT = 1;
constexpr auto RENDER_WIDTH = 1280;
constexpr auto RENDER_HEIGHT = 720;
constexpr auto SAVE_FORMAT = 0; // 0 == pfm; 1 == png srgb

#define INTERPOLATE_NORMALS true

